name: Build and Push OpenClaw Image

on:
  schedule:
    # Runs daily at 06:00 UTC -- checks for new OpenClaw releases
    - cron: "0 6 * * *"
  workflow_dispatch:
    inputs:
      force_version:
        description: "Force build a specific version tag (e.g., v2026.2.9). Leave empty to auto-detect latest."
        required: false
        default: ""

permissions:
  contents: write
  packages: write

env:
  IMAGE_NAME: ghcr.io/pwnapplehat/openclaw-image
  UPSTREAM_REPO: openclaw/openclaw

jobs:
  check-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Determine target version
        id: version
        run: |
          if [ -n "${{ github.event.inputs.force_version }}" ]; then
            TAG="${{ github.event.inputs.force_version }}"
          else
            TAG=$(curl -sf "https://api.github.com/repos/${{ env.UPSTREAM_REPO }}/releases/latest" \
              | jq -r '.tag_name')
          fi
          if [ -z "$TAG" ] || [ "$TAG" = "null" ]; then
            echo "::error::Could not determine upstream version"
            exit 1
          fi
          # Normalise: strip leading 'v' for the bare version, keep 'v' prefix for git tag
          BARE="${TAG#v}"
          echo "tag=v${BARE}" >> "$GITHUB_OUTPUT"
          echo "bare=${BARE}" >> "$GITHUB_OUTPUT"
          echo "Resolved upstream version: v${BARE}"

      - name: Check if we already built this version
        id: check
        run: |
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            "https://api.github.com/repos/${{ github.repository }}/releases/tags/${{ steps.version.outputs.tag }}")
          if [ "$STATUS" = "200" ] && [ -z "${{ github.event.inputs.force_version }}" ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
            echo "Version ${{ steps.version.outputs.tag }} already built. Skipping."
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Clone upstream at release tag
        if: steps.check.outputs.skip == 'false'
        run: |
          git clone --depth 1 --branch "${{ steps.version.outputs.tag }}" \
            "https://github.com/${{ env.UPSTREAM_REPO }}.git" openclaw-src

      - name: Set up Docker Buildx
        if: steps.check.outputs.skip == 'false'
        uses: docker/setup-buildx-action@v3

      - name: Log in to ghcr.io
        if: steps.check.outputs.skip == 'false'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        if: steps.check.outputs.skip == 'false'
        uses: docker/build-push-action@v6
        with:
          context: ./openclaw-src
          file: ./openclaw-src/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ steps.version.outputs.bare }}
            ${{ env.IMAGE_NAME }}:${{ steps.version.outputs.tag }}
            ${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Create GitHub Release
        if: steps.check.outputs.skip == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: "OpenClaw ${{ steps.version.outputs.bare }}"
          body: |
            Auto-built from [openclaw/openclaw@${{ steps.version.outputs.tag }}](https://github.com/${{ env.UPSTREAM_REPO }}/releases/tag/${{ steps.version.outputs.tag }}).

            
